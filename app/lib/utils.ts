export const formatRecentWork = (works) => { //finds unique event & collab names from recent performances, to display as hompage links

    const events = [];
    const collabs = [];
    const output = [];

    for(let i = 0; i < works.length; i++){

    }
};